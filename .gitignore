# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment
.env

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS
.DS_Store
Thumbs.db

# WhatsApp session credentials — contains private keys for the throwaway account
baileys_auth_info/

# Runtime memory file — gitignored; back up manually
data/approved-responses.md

# Claude Code local per-user settings — not for public repo
.claude/settings.local.json

# Real friend conversations produced by the compounding loop.
# Must be anonymized before any commit if ever shared.
approved-responses.md
